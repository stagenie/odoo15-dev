<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Menu principal de l'application (niveau racine) -->
    <menuitem
        id="menu_return_main"
        name="Retours"
        sequence="45"
        web_icon="adi_return_management,static/description/icon.png"
        groups="adi_return_management.group_return_user"/>

    <!-- ====================== -->
    <!-- Menu Operations        -->
    <!-- ====================== -->
    <menuitem
        id="menu_return_operations"
        name="Operations"
        parent="menu_return_main"
        sequence="10"/>

    <!-- Retours Clients -->
    <menuitem
        id="menu_return_order"
        name="Retours Clients"
        parent="menu_return_operations"
        action="action_return_order"
        sequence="10"/>

    <!-- ====================== -->
    <!-- Menu Reporting         -->
    <!-- ====================== -->
    <menuitem
        id="menu_return_reporting"
        name="Analyse"
        parent="menu_return_main"
        sequence="50"/>

    <!-- Tableau croise dynamique (Pivot) -->
    <menuitem
        id="menu_return_pivot"
        name="Tableau croise dynamique"
        parent="menu_return_reporting"
        action="action_return_order_pivot"
        sequence="10"/>

    <!-- Evolution des retours -->
    <menuitem
        id="menu_return_evolution"
        name="Evolution des retours"
        parent="menu_return_reporting"
        action="action_return_order_evolution"
        sequence="20"/>

    <!-- Retours par raison -->
    <menuitem
        id="menu_return_by_reason"
        name="Retours par raison"
        parent="menu_return_reporting"
        action="action_return_order_by_reason"
        sequence="30"/>

    <!-- Retours par client -->
    <menuitem
        id="menu_return_by_partner"
        name="Retours par client"
        parent="menu_return_reporting"
        action="action_return_order_by_partner"
        sequence="40"/>

    <!-- Retours par entrepot -->
    <menuitem
        id="menu_return_by_warehouse"
        name="Retours par entrepot"
        parent="menu_return_reporting"
        action="action_return_order_by_warehouse"
        sequence="50"/>

    <!-- ====================== -->
    <!-- Menu Configuration     -->
    <!-- ====================== -->
    <menuitem
        id="menu_return_config"
        name="Configuration"
        parent="menu_return_main"
        sequence="100"
        groups="adi_return_management.group_return_manager"/>

    <!-- Raisons de retour -->
    <menuitem
        id="menu_return_reason"
        name="Raisons de retour"
        parent="menu_return_config"
        action="action_return_reason"
        sequence="10"/>

    <!-- Parametres -->
    <menuitem
        id="menu_return_settings"
        name="Parametres"
        parent="menu_return_config"
        action="action_return_config_settings"
        sequence="99"/>

</odoo>
