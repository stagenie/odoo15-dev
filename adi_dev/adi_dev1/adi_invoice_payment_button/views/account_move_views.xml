<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Ajout du Smart Button sur la vue formulaire des factures -->
    <record id="view_move_form_payment_button" model="ir.ui.view">
        <field name="name">account.move.form.payment.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_payments"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-money"
                        attrs="{'invisible': ['|', ('payment_count', '=', 0), ('move_type', 'not in', ['out_invoice', 'out_refund', 'in_invoice', 'in_refund'])]}">
                    <field name="payment_count" widget="statinfo" string="Paiements"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
