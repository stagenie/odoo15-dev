<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Form view for merge picking wizard -->
    <record id="merge_picking_view_form" model="ir.ui.view">
        <field name="name">merge.picking.view.form</field>
        <field name="model">merge.picking</field>
        <field name="arch" type="xml">
            <form string="Merge Pickings">
                <sheet>
                    <p>Lorsque vous sélectionnez des bons pour les regrouper,
                        <br/>
                        * Le regroupement n'est pas permis pour un seul bon ainsi que pour 
                        les bons faits (validés) 
                        <br/>
                        * Les transferts doivent être avec le même partenaire, les mêmes types,
                        et les mêmes états
                    </p>
                    <field name="merge_picking_ids">
                        <tree>
                            <field name="name"/>
                            <field name="partner_id"/>
                            <field name="origin"/>
                            <field name="scheduled_date"/>
                            <field name="state"/>
                        </tree>
                    </field>
                    <group>
                        <field name="existing_pick_id"
                               domain="[('id', 'in', merge_picking_ids)]"
                               widget="selection"/>
                    </group>
                    <footer>
                        <button name="action_merge" string="Regrouper" type="object"
                                class="btn-primary"/>
                        <button string="Annuler" class="btn-secondary"
                                special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
